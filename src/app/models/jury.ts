export class JuryManager {
  juryID!: number;
  expertiseArea!: string;
  imageUrl!: string; // Correction de la propriété 'imageUrl'

  diploma!: string;
  firstName!: string;
  lastName!: string;
  email!: string;
  password!: string;
  telNumber!: string;
  archived!: boolean;
}
